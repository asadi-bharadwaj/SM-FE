import { EmptyState } from '../components/common/EmptyState'

export function NotificationsPage() {
  return <EmptyState title="Notifications" description="Likes, comments, and subscription activity will appear here (UI mock)." />
}
