/**
 * /messages index — layout shows inbox (mobile) or split+placeholder (desktop).
 * This child renders nothing; kept for a stable route tree.
 */
export function MessagesInboxPage() {
  return null
}
